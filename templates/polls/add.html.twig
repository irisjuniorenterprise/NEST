<div class="container">
    {{ form_start(postForm,{'attr':{ 'action':path('app_poll_update_submit', { id: poll.post.id , pollId:poll.id })}}) }}
    {{ form_start(pollForm,{'attr':{ 'action':path('app_poll_update_submit', { id: poll.id})}}) }}
    {{ form_start(pollOptionForm,{'attr':{ 'action':path('app_poll_update_submit', { id: poll.id})}}) }}
    {{ form_widget(postForm) }}
    {{ form_widget(pollForm) }}
    {{ form_widget(pollOptionForm) }}

    <input type="submit" style="display :inline! important ; " class="btn btn-primary data-submit me-1">
    {{ form_end(postForm) }}
    {{ form_end(pollForm) }}
    {{ form_end(pollOptionForm) }}
</div>